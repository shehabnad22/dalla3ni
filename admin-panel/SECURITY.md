# 🔐 نظام الأمان - Admin Panel

## بيانات الدخول المحددة

**البريد الإلكتروني:** `shehab.nad22@gmail.com`  
**كلمة المرور:** `Ss123456789`

## الحماية المطبقة

### 1. التحقق في الواجهة (Frontend)
- يتم التحقق من البريد وكلمة المرور قبل إرسال الطلب للـ API
- يتم حفظ البريد في localStorage للتحقق في كل طلب
- إذا كان البريد غير صحيح، يتم منع الدخول فوراً

### 2. التحقق في الـ Backend
- يتم التحقق من البيانات في `/api/auth/admin/login`
- فقط البريد `shehab.nad22@gmail.com` والرمز `Ss123456789` مسموحان
- إذا لم يكن هناك حساب admin، يتم إنشاؤه تلقائياً

### 3. التحقق المستمر
- في كل طلب API، يتم التحقق من البريد المحفوظ
- إذا تغير البريد، يتم تسجيل الخروج تلقائياً

## ملاحظات الأمان

⚠️ **مهم:** 
- لا تشارك بيانات الدخول مع أي شخص
- في الإنتاج، استخدم HTTPS
- فكر في إضافة 2FA (Two-Factor Authentication)
- يمكن تغيير كلمة المرور من قاعدة البيانات

## تغيير بيانات الدخول

للتغيير، عدّل في:
1. `admin-panel/app.js` - السطرين `ADMIN_EMAIL` و `ADMIN_PASSWORD`
2. `backend/src/routes/auth.js` - السطرين `ADMIN_EMAIL` و `ADMIN_PASSWORD`

